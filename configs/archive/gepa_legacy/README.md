# Archived GEPA Configs

These configs are retained for provenance. Do not use them for new GEPA rule
optimization runs unless you are intentionally reproducing a historical setup.

| Config | Reason archived |
|---|---|
| `gepa_verified_rules_formal_pilot_pre_promptfix.yaml` | Local Docker formal pilot before the current prompt-fix naming and 3000-call local validation setup. |
