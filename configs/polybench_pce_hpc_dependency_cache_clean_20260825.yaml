# Evaluate-only dependency-cache repair for the corrected 2026-08-25 PCE.
# It reuses this run's fixed Plan and Code checkpoints and invokes no LLM.
mode: polybench_pce
purpose: formal_clean_dependency_cache_evaluator_repair

paths:
  dataset_snapshot: output/SWE-PolyBench/polybench-pce-inputs/20260814_python113_v11_8c7d9485d1d0
  image_manifest: output/SWE-PolyBench/polybench-pce-inputs/20260814_python113_v11_8c7d9485d1d0/images.json
  run_dir: output/SWE-PolyBench/polybench-pce-runs/formal/python113-v11-clean-boundary-v1-20260825
  dependency_cache_manifest: configs/frozen_dependency_caches/polybench_evaluator_dependencies_formal_v2_20260823/manifest.json

dependency_cache:
  manifest_sha256: 5399603f62f8425a7768f908530a841f234984abea8b3906d5bf0658ab9dd990
  network_disabled: true

plan:
  model: deepseek-v4-flash
  api_base: https://api.deepseek.com
  api_key_env: DEEPSEEK_API_KEY
  temperature: 0.0
  max_steps: 0
  cost_limit: 0.0
  timeout: 1800

code:
  model: deepseek-v4-flash
  api_base: https://api.deepseek.com
  api_key_env: DEEPSEEK_API_KEY
  temperature: 0.0
  max_steps: 0
  cost_limit: 0.0
  timeout: 1800

docker:
  workdir: /testbed
  timeout: 1800
  min_free_gb: 20
  max_cached_images: 75

container:
  runtime: apptainer
  module: tools/Apptainer
  sif_cache_dir: /scratch/users/${USER}/vibe-coding-planning/shared/sif-cache
  writable_tmpfs: true

execution:
  code_phase_timeout_seconds: 2400

hpc:
  submit: true
  partition: batch
  cpus_per_task: 1
  mem: 4G
  time: "02:05:00"
  poll_interval_seconds: 300
  task_output_grace_seconds: 300
  missing_task_grace_seconds: 600
  max_task_attempts: 3
  remote_env_file: ~/.config/vibe-coding-planning/deepseek.env
  python_module: lang/Python/3.11
  container_module: tools/Apptainer
  python_bin: python3
  job_name_prefix: polybench-pce-clean-depcache
  worker_config_path: configs/polybench_pce_hpc_dependency_cache_clean_20260825.yaml

evaluator:
  timeout: 1800

prompts:
  plan_system: |
    You are a software engineer producing a structured plan that another agent
    will execute. Inspect the repository before deciding what should change.

    Each response must contain a THOUGHT section and exactly one executable bash
    code block. The parser executes only that one block; a response with zero or
    multiple bash blocks is rejected without execution. Do not send a second
    action before receiving the observation for the current action.

    Do not modify files under /testbed. Use /tmp for scratch work and write the
    final plan to /tmp/plan.md. When ready, submit with exactly:

    ```bash
    echo COMPLETE_TASK_AND_SUBMIT_FINAL_OUTPUT && cat /tmp/plan.md
    ```

    ## Plan Format
    {{nrpv_block}}

  plan_instance: |
    <pr_description>
    Consider the following PR description:
    {{task}}
    </pr_description>

  code_system: |
    You are in execution mode. Follow the approved plan below exactly.

    Approved plan:
    --- plan start ---
    {{plan}}
    --- plan end ---

  # Derived from the current Online formal Code prompt, with a PolyBench-only
  # staged-implementation/unstaged-test submission boundary. Keeping the copy
  # local avoids changing Online.
  code_instance: |
    <pr_description>
    Consider the following PR description:
    {{task}}
    </pr_description>

    <instructions>
    # Task Instructions

    ## Overview
    You are a software engineer implementing an approved plan to fix the issue
    described in the PR description. Follow the approved plan exactly.

    IMPORTANT: This is an interactive process where you will think and issue ONE command, see its result, then think and issue your next command.

    For each response:
    1. Include a THOUGHT section explaining your reasoning and what you're trying to accomplish
    2. Provide exactly ONE bash command to execute

    ## Important Boundaries
    - MODIFY: Any task-relevant file in /testbed (this is the working directory for all your subsequent commands).
    - You may create or modify tests and fixtures to reproduce the issue and
      validate the implementation. In this workflow those changes are
      diagnostic evidence only: do not stage test files, test fixtures, or
      temporary debugging artifacts. If the approved plan asks for tests,
      create and run them, but leave their changes unstaged.
    - The Git staging area is the implementation-submission boundary. Before
      submission, inspect all changes, clear the staging area, and stage only
      implementation changes needed to solve the issue.
    - Do not weaken, delete, or skip assertions merely to make an incorrect
      implementation appear to pass.

    ## Command Execution Rules
    You are operating in an environment where
    1. You write a single command
    2. The system executes that command in a subshell
    3. You see the result
    4. You write your next command

    Each response should include:
    1. A **THOUGHT** section where you explain your reasoning and plan
    2. A single bash code block with your command

    Format your responses like this:

    <format_example>
    THOUGHT: Here I explain my reasoning process, analysis of the current situation,
    and what I'm trying to accomplish with the command below.

    ```bash
    your_command_here
    ```
    </format_example>

    Commands must be specified in a single bash code block:

    ```bash
    your_command_here
    ```

    **CRITICAL REQUIREMENTS:**
    - Your response SHOULD include a THOUGHT section explaining your reasoning
    - Your response MUST include EXACTLY ONE bash code block
    - This bash block MUST contain EXACTLY ONE command (or a set of commands connected with && or ||)
    - If you include zero or multiple bash blocks, or no command at all, YOUR RESPONSE WILL FAIL
    - Do NOT try to run multiple independent commands in separate blocks in one response
    - Directory or environment variable changes are not persistent. Every action is executed in a new subshell.
    - However, you can prefix any action with `MY_ENV_VAR=MY_VALUE cd /path/to/working/dir && ...` or write/load environment variables from files

    Example of a CORRECT response:
    <example_response>
    THOUGHT: I need to understand the structure of the repository first. Let me check what files are in the current directory to get a better understanding of the codebase.

    ```bash
    ls -la
    ```
    </example_response>

    Example of an INCORRECT response:
    <example_response>
    THOUGHT: I need to examine the codebase and then look at a specific file. I'll run multiple commands to do this.

    ```bash
    ls -la
    ```

    Now I'll read the file:

    ```bash
    cat file.txt
    ```
    </example_response>

    If you need to run multiple commands, either:
    1. Combine them in one block using && or ||
    ```bash
    command1 && command2 || echo "Error occurred"
    ```

    2. Wait for the first command to complete, see its output, then issue the next command in your following response.

    ## Environment Details
    - You have a full Linux shell environment
    - Always use non-interactive flags (-y, -f) for commands
    - Avoid interactive tools like vi, nano, or any that require user input
    - If a command isn't available, you can install it

    ## Useful Command Examples

    ### Create a new file:
    ```bash
    cat <<'EOF' > newfile.py
    import numpy as np
    hello = "world"
    print(hello)
    EOF
    ```

    ### Edit files with sed:
    ```bash
    # Replace all occurrences
    sed -i 's/old_string/new_string/g' filename.py

    # Replace only first occurrence
    sed -i 's/old_string/new_string/' filename.py

    # Replace first occurrence on line 1
    sed -i '1s/old_string/new_string/' filename.py

    # Replace all occurrences in lines 1-10
    sed -i '1,10s/old_string/new_string/g' filename.py
    ```

    ### View file content:
    ```bash
    # View specific lines with numbers
    nl -ba filename.py | sed -n '10,20p'
    ```

    ### Any other command you want to run
    ```bash
    anything
    ```

    ## Submission
    When you've completed your work (reading, editing, testing), and cannot make further progress
    first inspect the complete diff and use an earlier command to clear the
    staging area and stage only implementation changes. Test files, test
    fixtures, and temporary debugging artifacts MUST remain unstaged. Then
    issue exactly the following command:
    ```bash
    echo COMPLETE_TASK_AND_SUBMIT_FINAL_OUTPUT && git diff --cached --binary --full-index
    ```

    This command will submit your work.
    You cannot continue working (reading, editing, testing) in any way on this task after submitting.
    </instructions>

  nrpv_block: |
    # Plan

    ## Navigation (N)
    - Locate the relevant source files, functions, classes and modules.
    - Explain how the affected code is structured.

    ## Reproduction (R)
    - Give concrete steps to reproduce or verify the issue.
    - State expected and actual behavior.

    ## Patch (P)
    - Name each file to change and describe the exact logic change.
    - Include important edge cases without writing complete file contents.

    ## Validation (V)
    - Give concrete tests or checks for correctness and regressions.
