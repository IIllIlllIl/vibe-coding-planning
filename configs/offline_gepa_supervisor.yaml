# Run the conservative-seed eight-iteration formal Offline experiment.
schema_version: 1
session: offline-plan-guideline-hpc-accuracy-b12-8it-explicit-turn-contract-formal-20260805
log: .local/hpc-supervisor/offline-plan-guideline-hpc-accuracy-b12-8it-explicit-turn-contract-formal-20260805.log
arguments:
  # The cumulative target is read from search.max_iterations in the Offline
  # runtime config. Supervisor is the only resume entrypoint.
  - --poll-interval
  - "600"
  - --slice-time
  - "00:10:00"
  - --max-runs
  - "0"
  - --require-clean-worktree
  - --gepa-rules
  - --gepa-config
  - configs/gepa_verified_rules.yaml
  - --job-name
  - offline-plan-guideline-hpc-accuracy-b12-8it-explicit-turn-contract-formal-20260805
  - --remote-dir
  - ~/hpc_runs/vibe-coding-planning-offline-guideline-explicit-turn-contract-8it-formal-20260805
  - --cpus
  - "1"
  - --mem
  - 4G
  - --submit
