# Core dependencies
mini-swe-agent==1.17.5
swebench>=4.1.0
pyyaml>=6.0.0

# PolyBench evaluation (official harness)
poly_bench_evaluation @ git+https://github.com/amazon-science/SWE-PolyBench.git@1eb0bdc8ef63e1e88172b96bc435b1cd9fc93ecc
# Install non-editably from GitHub, or editable from a persistent checkout.
# Never editable-install from /tmp: cleanup can leave a false-positive namespace
# package while deleting docker_utils/scoring during a long batch.
#   pip install "poly_bench_evaluation @ git+https://github.com/amazon-science/SWE-PolyBench.git@1eb0bdc8ef63e1e88172b96bc435b1cd9fc93ecc"
# Required exact versions for tree-sitter compatibility:
#   tree-sitter==0.21.3
#   tree-sitter-languages==1.10.2

# LLM API (DeepSeek via OpenAI-compatible API)
openai>=2.24.0
litellm>=1.83.0

# Development / testing
pytest>=8.0.0
pytest-cov>=5.0.0

# Optional: used only for extracting the reflection prompt template
# from GEPA during development. Not required at runtime.
# gepa>=0.1.1
