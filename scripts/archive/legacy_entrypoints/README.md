# Archived entrypoints

These scripts are retained for historical reproducibility only. New experiments
must start through `scripts/run_batch.sh` or `scripts/long_run_watchdog.py`.

Do not add new production references to files in this directory.
