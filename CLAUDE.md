# Claude Code Notes

`AGENTS.md` is the operational authority for this branch. Read it completely
before acting, followed by `README.md`, `docs/branch-scope.md`, and
`project_issues.md`.

The active research path is SWE-chat Behavioral Plan Acceptability built on the
retained Offline GEPA implementation and frozen clean PolyBench PCE/PCCE
evidence. Online GEPA, PCT/PCC, Pro, and old analysis paths are historical even
when temporarily retained for reachability analysis. Do not browse archive
trees unless the task explicitly requires audit, comparison, or reproduction.

Use `conda run -n mini-swe` for Python. Never expose credentials, never delete
`.claude/`, and do not start an LLM, GEPA, Docker, Apptainer, HPC, PCE, PCCE,
OpenCode, or long-running test without explicit authorization. Do not modify
frozen evidence or `third_party/gepa` in place.
